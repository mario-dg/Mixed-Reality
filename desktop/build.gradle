apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'wpcg.CG3D'

repositories {
    mavenCentral()
    jcenter()
}

def jme3 = [v: '3.3.2-stable', g: 'org.jmonkeyengine']

dependencies {
    implementation "${jme3.g}:jme3-core:${jme3.v}"
    implementation "${jme3.g}:jme3-effects:${jme3.v}"
    implementation "${jme3.g}:jme3-plugins:${jme3.v}"
    implementation "${jme3.g}:jme3-desktop:${jme3.v}"
    implementation "${jme3.g}:jme3-lwjgl:${jme3.v}"
    implementation 'org.json:json:20190722'
    testImplementation('org.junit.jupiter:junit-jupiter:5.6.2')
    // Tinkerforge: can be used in final project
    implementation 'com.tinkerforge:tinkerforge:2.1.30'
    // OpenCV: can be used in final project
    implementation 'org.openpnp:opencv:4.5.1-2'
}

test {
    useJUnitPlatform()
    testLogging {
        events "passed", "skipped", "failed"
    }
}